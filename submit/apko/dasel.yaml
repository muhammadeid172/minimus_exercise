contents:
  repositories:
    - ./packages
    - https://packages.wolfi.dev/os

  keyring:
    - ./melange.rsa.pub
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub

  packages:
    - busybox
    - dasel

entrypoint:
  command: /usr/bin/dasel